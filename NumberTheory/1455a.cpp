#include<bits/stdc++.h>
using namespace std;
// the answers changes for each power of 10. ie count the number of 10^k in the given number
int main() {
    int t; cin>>t;
    while(t--) {
        string st;
        cin>>st;
        int ans = st.length();
        cout<<ans<<endl;
    }
}